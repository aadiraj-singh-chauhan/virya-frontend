import NotFound from '@/components/not-found/components/NotFound';

export const metadata = {
  title: '404 · Virya',
  description: 'The page you are looking for doesn\'t exist.',
};

export default function NotFoundPage() {
  return <NotFound />;
}
